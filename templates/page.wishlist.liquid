{% include 'breadcrumbs' %}
<div class="page-wishlist mb-40">
    {%- if customer -%}
        {% include 'wishlist' %}
    {%- else -%}
        <script>
            window.location.href = '/account/login';
        </script>
    {%- endif -%}
</div>