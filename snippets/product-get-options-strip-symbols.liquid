{% assign symbols = '-+!"#$€₹%&* ,./:;<=>?@[\\]_`{|}~()°^' | append: "'" | split: '' -%}
{%- assign strip_value = value -%}
{%- for symbol in symbols -%}
    {%- assign strip_value = strip_value | remove: symbol | downcase -%}
{%- endfor -%}
{{- strip_value }}